# @google/model-viewer peers three@^0.183 while this project deliberately pins
# three@0.170 (the R3F stack is built against it — see components/car/ARCarViewer.tsx).
# Without this, a plain `npm install` fails with ERESOLVE.
legacy-peer-deps=true
